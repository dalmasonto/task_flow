//! URL conf for the taskflow-github plugin. Populated with real routes + state
//! in Task 5 onward; empty until handlers exist.
use umbral::web::Router;

pub fn router() -> Router {
    Router::new()
}
