/target
/backend.db*
.env
data.json
# collectstatic output + backup_media's filesystem target — artifacts, not source.
/staticfiles
/media_backup
# CI builds with --locked, so Cargo.lock is COMMITTED (binaries pin deps).
# dist/ holds the CI-built release binary staged for the Docker image.
/dist
