# Rust
target/
**/target/

# Databases (local dev state)
*.db
*.db-shm
*.db-wal

# Node
node_modules/
dist/
.vite/

# Env / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.swp

# v1 (tauri + sqlite) — has its own git repo
task_flow/
.screenshots/

# Uploaded media (runtime data, served from ./media)
backend/media/
media/
