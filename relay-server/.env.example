# Relay server configuration
PORT=4000

# Token used by the local MCP server to push events
PUSH_TOKEN=abcdefgh

# Token used by remote clients to access the relay
ACCESS_TOKEN=123456

# Optional: pre-configure upstream URL (otherwise set via /register)
# UPSTREAM_URL=http://localhost:3456
