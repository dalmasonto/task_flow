## Features to add


1. The debug logs should use complete markdown and always keep whitespace. Should display in full as they do now
2. Since some logs are linked to tasks, both the task view and the dependency page task sheet, should show the debug logs for that task
3. ~~Add more instructions to the MCP server function~~ ✅ Done — added multi-agent collaboration rules (identity, discovery, communication, coordination pattern, handoffs, conflict avoidance, guided app building), plus enhanced startup steps and tips. Task creation/logging rules were already covered.